import 'package:flutter/material.dart';

class ReservationTwoScreen extends StatelessWidget {
  const ReservationTwoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Reservation 2 Screen"),
      ),
    );
  }
}
